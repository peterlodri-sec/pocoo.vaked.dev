# EVA-NIMFA — entheai fan-out scaffold (restoration)
# Feed this to: entheai --fanout --config <entheai.toml> "<the eva-nimfa brief>"
# entheai (deepseek via openrouter, or local HF-mac model) decomposes +
# integrates; each [[coder]] runs via the Antigravity CLI (agy).

[book]
title = "ÉVA"
subtitle = "restored from the original manuscript"
author = "Szellem (Brett Shaw)"
epigraph = ""
published = 2026

[fanout]
executor = "agy"                     # coder backend (Antigravity CLI)
model = "openrouter/deepseek/deepseek-chat"  # entheai's brain (deepseek)
agy_model = "gemini-3.6-flash-high"  # model agy runs each coder on
max_depth = 3

# Tone for every scribe: keep every original line of the manuscript. This is
# a restoration — polish for book form, never discard the source.

# ─── FRONT MATTER ────────────────────────────────────────────────────────

[[coder]]
role = "scribe"
chapter = 0
file = "chapters/00-dedication.md"
task = """
Write the front matter: dedication page, epigraph,
and a one-page prologue introducing the book. Every original line of the
manuscript is sacred — restore it verbatim. Tone: match the source.
"""

# ─── CHAPTERS ────────────────────────────────────────────────────────────

[[coder]]
role = "scribe"
chapter = 1
file = "chapters/01-eva.md"
task = """
SEED: the "ÉVA" section of the original manuscript.html.
Rewrite/expand it for book form. KEEP ALL ORIGINAL LINES — restoration, not
replacement. Match the book's voice.
"""
depends_on = [0]

[[coder]]
role = "scribe"
chapter = 2
file = "chapters/02-1-a-belepes.md"
task = """
SEED: the "1. A Belépés" section of the original manuscript.html.
Rewrite/expand it for book form. KEEP ALL ORIGINAL LINES — restoration, not
replacement. Match the book's voice.
"""
depends_on = [1]

[[coder]]
role = "scribe"
chapter = 3
file = "chapters/03-2-a-mezitelenseg.md"
task = """
SEED: the "2. A Mezítelenség" section of the original manuscript.html.
Rewrite/expand it for book form. KEEP ALL ORIGINAL LINES — restoration, not
replacement. Match the book's voice.
"""
depends_on = [2]

[[coder]]
role = "scribe"
chapter = 4
file = "chapters/04-3-a-testver.md"
task = """
SEED: the "3. A Testvér" section of the original manuscript.html.
Rewrite/expand it for book form. KEEP ALL ORIGINAL LINES — restoration, not
replacement. Match the book's voice.
"""
depends_on = [3]

[[coder]]
role = "scribe"
chapter = 5
file = "chapters/05-4-az-oleles-ami-nem-tortent-meg.md"
task = """
SEED: the "4. Az Ölelés, Ami Nem Történt Meg" section of the original manuscript.html.
Rewrite/expand it for book form. KEEP ALL ORIGINAL LINES — restoration, not
replacement. Match the book's voice.
"""
depends_on = [4]

[[coder]]
role = "scribe"
chapter = 6
file = "chapters/06-5-a-nimfa.md"
task = """
SEED: the "5. A Nimfa" section of the original manuscript.html.
Rewrite/expand it for book form. KEEP ALL ORIGINAL LINES — restoration, not
replacement. Match the book's voice.
"""
depends_on = [5]

[[coder]]
role = "scribe"
chapter = 7
file = "chapters/07-6-a-hala.md"
task = """
SEED: the "6. A Hála" section of the original manuscript.html.
Rewrite/expand it for book form. KEEP ALL ORIGINAL LINES — restoration, not
replacement. Match the book's voice.
"""
depends_on = [6]

[[coder]]
role = "integrator"
file = "EVA-NIMFA.md"
task = """
INTEGRATE the front matter + all 7 chapters into a single manuscript.
Verify every original line survived. Add part dividers where the source has
them. Add the dedication page first and the epigraph.
Add colophon: "Restored through the fan-out, 2026." Output as one clean
markdown file ready for print.
"""
depends_on = [0, 1, 2, 3, 4, 5, 6]
